Preparation - install packages:

1) go to root path and start "npm i" to install electron package

2) change direction to source in "angular-electron-demo" folder
	- start "npm i" to install app packages

-----------------------------------------------------------------

How to start the application as electron or as web app:

Start electron: "npm run electron"

Start Web App: "npm run start" or "ng serve"